/*
 * A stack data structure, follows the rule "last in first out".
 */
class Stack {

    function size() {
        abstract;
    }

    function top() {
        abstract;
    }

    function push(element) {
        abstract;
    }

    function pop() {
        abstract;
    }
}
